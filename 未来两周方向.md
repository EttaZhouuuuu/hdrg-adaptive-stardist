# Shape-Aware StarDist++：引入 Instance Separation Head 的神经元分割模型

本项目在已有的 **Adaptive Shape-Aware StarDist** 框架基础上，引入一个显式的 **Instance Separation Geometry Head**，用于提升神经元胞体（neuron soma）分割在跨 slide、跨 stain、跨成像条件下的泛化能力。

该设计的目标并非针对某一固定数据集进行过拟合，而是解决一个结构性失败模式——**相邻实例的粘连合并（touching merge）**，该问题在 domain shift 条件下尤为突出。

---

## 1. 研究动机

### 问题背景
DRG 神经元胞体分割面临以下挑战：
- 胞体形状高度不规则，存在明显 concavity；
- 神经元密集排列，实例间频繁接触；
- 不同 slide 与实验条件下染色与对比度差异显著。

尽管 Adaptive StarDist 在 in-domain 数据上性能较好，但系统误差分析显示，其主要失败模式之一是：

> **相邻 neuron soma 被错误合并为一个实例**，在低对比度或高密度区域尤为明显。

该问题具有明显的跨 domain 共性。

### 核心洞察
StarDist 能有效建模单实例形状（radial distances），但在实例接触时缺乏显式的 **instance separability geometry**。当 boundary cue 因 domain shift 退化时，merge error 显著增加。

**研究假设：**  
若模型能够显式学习“像素指向所属实例中心”的几何一致性信号，则可在 boundary 不清晰或 domain shift 场景下保持更稳定的实例分离能力。

---

## 2. 方法概览

本工作引入一个 **Instance Separation Head**，预测每个前景像素到其所属实例中心的 **offset vector field**，并与 StarDist 的形状重建机制解耦融合。

该 head 并不替代 StarDist，而是专注于实例分离。

### 模型结构
扩展后的模型包含：

- **Backbone（保持不变）**
  - Multi-scale FPN
  - Transformer-based shape prior encoder
  - Deformable convolution refinement

- **原有 heads**
  - Foreground probability head
  - Radial distance head（StarDist-style）
  - Shape complexity head

- **新增 head**
  - **Instance offset head**：输出二维 offset \((dx, dy)\)

所有 heads 共享同一高分辨率特征图。

---

## 3. Instance Offset 表征

### 定义
对于属于实例 \(k\) 的像素 \(p=(x,y)\)，实例中心为 \(c_k=(x_k,y_k)\)，其 offset 定义为：

\[
O_{gt}(p) = \left(\frac{x_k - x}{s_k}, \frac{y_k - y}{s_k}\right)
\]

其中 \(s_k\) 为尺度归一化因子（如 equivalent radius 或 bounding box diagonal）。  
offset 仅在前景像素上定义。

### 设计理由
- 同一实例内像素具有一致的几何指向性；
- 接触实例在 offset 场中自然产生分歧；
- offset 基于空间结构而非外观，对 domain shift 更稳健。

---

## 4. Loss 设计

在原有 StarDist loss 基础上，引入实例分离约束。

### Offset Regression Loss
在前景像素上计算 Smooth L1 loss：

\[
L_{off} = \text{SmoothL1}(O_{pred}, O_{gt})
\]

### 接触区域加权
为重点优化粘连区域：
1. 对 instance mask 做轻微 dilation；
2. 不同实例 dilation 的重叠区域定义为 *touching zones*；
3. 在该区域提高 offset loss 权重。

### 总 Loss
\[
L = \alpha_p L_{prob}
  + \alpha_d L_{dist}
  + \alpha_c L_{complex}
  + \alpha_{off} L_{off}^{weighted}
  + L_{regularization}
\]

---

## 5. 推理流程

offset head 负责 **实例分离**，StarDist distance 负责 **边界细化**。

1. **Foreground masking**：由 probability head 生成；
2. **Center voting**：  
   \(c(p) = p + s(p)\cdot O_{pred}(p)\)
3. **Seed detection**：在 center space 中寻找局部极值；
4. **Instance separation**：使用 seeded watershed 或 clustering；
5. **Boundary refinement**：利用 StarDist radial distances 重建最终边界。

---

## 6. 训练策略

- 使用现有 neuron soma instance annotations；
- offset 标签由 GT mask 自动生成；
- 训练流程与 base model 一致；
- 仅使用通用 augmentation（rotation、scaling、noise）。

---

## 7. 评估方案

### 标准指标
- IoU、Dice、Precision、Recall。

### 分离相关指标
- **Merge rate**
- **Split rate**
- 在整体数据及 touching-instance 子集上统计。

### 泛化评估
通过 synthetic domain shift（contrast、blur、noise）测试推理阶段鲁棒性。  
性能退化幅度越小，泛化能力越强。

---

## 8. 预期效果

- 显著降低 touching neurons 的 merge error；
- domain shift 下性能更稳定；
- 无需针对单一数据集调参即可保持较高 IoU；
- 清晰区分 shape modeling 与 instance separability 的作用。

---

## 9. 局限性

- 主要解决 instance separation，不处理语义误分类；
- 极端重叠或缺乏结构信息的区域仍具挑战。

---

## 10. 总结

本项目在 shape-aware StarDist 框架中引入显式的 **instance separation geometry**，通过解耦边界建模与实例分离，提高模型在跨 domain 场景下的稳健性与泛化能力。

该改动结构清晰、可解释性强，并与现有 StarDist pipeline 高度兼容。
